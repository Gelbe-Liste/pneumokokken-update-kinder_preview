# Update 08.09.2026 – Piano Analytics / Mastertemplate v5

Tracking-only Migration des laufenden Projekts `pneumokokken-update-kinder_v0.7` auf den Analytics-Stand des med.i.scroll-Mastertemplates v5.

- zentrale Piano-v5-Tracking-Schicht übernommen
- `project.meta.projectId` und `project.meta.analytics` ergänzt
- `page.display`, `click.action`, `pop_in.display` wiederverwendet
- `chapter.display`, `page.scroll`, `video.start/progress/complete` eingeführt
- alte Eventnamen (`chapter_view`, `scroll_depth`, `module_complete`, `image_view_*`, `outbound_click`, `navigation_click`, `pdf_generate_*`, alte `video_*`) entfernt
- CTA-/CME-Klicks auf `click.action` + stabile `element_id`/bereinigte `destination_path` migriert
- PDF Generate/Download, Grafik Open/Close/Zoom, Kapitelmenü und Navigation harmonisiert
- redaktionelle Inhalte, Reihenfolge, Grafiken, Layout und PDF-Inhalt unverändert
