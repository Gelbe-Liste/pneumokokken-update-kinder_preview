{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "rm -f package-lock.json && npm install --include=optional --no-audit --no-fund",
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}
